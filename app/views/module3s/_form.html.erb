<%= form_for(@module3) do |f| %>
  <% if @module3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module3.errors.count, "error") %> prohibited this module3 from being saved:</h2>

      <ul>
      <% @module3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
