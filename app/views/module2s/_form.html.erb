<%= form_for(@module2) do |f| %>
  <% if @module2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module2.errors.count, "error") %> prohibited this module2 from being saved:</h2>

      <ul>
      <% @module2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
