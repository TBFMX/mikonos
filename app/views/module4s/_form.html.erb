<%= form_for(@module4) do |f| %>
  <% if @module4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module4.errors.count, "error") %> prohibited this module4 from being saved:</h2>

      <ul>
      <% @module4.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
