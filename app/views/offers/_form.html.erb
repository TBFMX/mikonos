<%= form_for(@offer) do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :offer_price %><br>
    <%= f.text_field :offer_price %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= select("offer", "product_id", Product.all.collect {|p| [ p.title, p.id ] }, {include_blank: 'Seleccione uno'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
